import { style } from '@vanilla-extract/css';

export const libraryEntry = style({
  display: 'flex',
  alignItems: 'center',
});

export const close = style({
  padding: 0,
  border: 0,
  backgroundColor: 'transparent',
});
